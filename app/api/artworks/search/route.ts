import{NextRequest,NextResponse}from'next/server';import{searchArtworks}from'@/services/search';export async function GET(req:NextRequest){const s=req.nextUrl.searchParams;return NextResponse.json(searchArtworks({q:s.get('q')||'',category:s.get('category')||undefined,mood:s.get('mood')||undefined,colour:s.get('colour')||undefined,style:s.get('style')||undefined,page:Number(s.get('page')||1),limit:Number(s.get('limit')||24),sort:s.get('sort')||undefined}))}
