import{NextRequest,NextResponse}from'next/server';import{artworks,orders,sellers}from'@/mock/data';export async function GET(_:NextRequest,{params}:{params:Promise<{id:string}>}){const{id}=await params;return NextResponse.json({seller:sellers.find(s=>s.id===id),artworks:artworks.filter(a=>a.sellerId===id),orders:orders.filter(o=>o.sellerId===id),isolation:'Only records matching authenticated sellerId are returned'})}
